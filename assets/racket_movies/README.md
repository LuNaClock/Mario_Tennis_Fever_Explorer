# racket_movies

このフォルダにはラケット効果確認用の mp4 を配置します。

- `fire_racket_demo.mp4` はリポジトリには含めていません（バイナリ非対応のため）。
- 本番運用時は実際の mp4 に差し替えてください。
- 読み込み先は `data.js` の `video.src`（例: `assets/racket_movies/fire_racket_demo.mp4`）です。
